# mailer

## Setup application
```bash
virtulenv venv
source venv/bin/active
pip install -e .
python -m mailer
```

You server should be started by now.

## Testing
```bash
pip install -r tests/requirements.txt
pytest
```
